# MySQL profile for running the backend against a MySQL server.
spring.datasource.url=jdbc:mysql://localhost:3306/poo?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=postgres
spring.datasource.password=salmamp12
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Update the schema instead of recreating it so persisted data is kept.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false
